//import statements
import { Button } from "react-bootstrap";

function AddEmployeeButton(){
    return(
        <Button onClick={addOne}>Add Employee</Button>
    );
}

export default AddEmployeeButton;